package net.lintfree.shroomscape.util

import net.lintfree.shroomscape.ShroomScape
import net.minecraft.resources.ResourceLocation
import net.minecraft.tags.BlockTags
import net.minecraft.tags.TagKey
import net.minecraft.world.level.block.Block

/*
class ModTags {
	class Blocks {
		companion object {
			val NEEDS_SHROOM_KNIFE: TagKey<Block> = createTag("needs_shroom_knife")
			val INCORRECT_FOR_SHROOM_KNIFE: TagKey<Block> = createTag("incorrect_for_shroom_knife")

			private fun createTag(name: String): TagKey<Block> {
				return BlockTags.create(ResourceLocation.fromNamespaceAndPath(ShroomScape.ID, name))
			}
		}
	}
}

 */

 

